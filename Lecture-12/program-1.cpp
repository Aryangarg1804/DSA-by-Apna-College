
#include<iostream>
using namespace std;
int main(){

    // Common time complexity.

    
    // O(1);
    // O(log(n));
    // O(n);
    // O(nlog(n));
    // O(n^2);
    // O(2^n);
    // O(n!);
    

    return 0;
}